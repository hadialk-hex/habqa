## 2026-07-11T09:28:49Z

You are teamwork_preview_worker. Your working directory is c:\Users\pc\Desktop\face bot\.agents\worker_m5_oauth_remediate\.
Mission: Remediation of Facebook OAuth & Credentials implementation. Resolve all integrity violations and security bypasses, fix E2E mocks and tests, and update encryption robustness tests.

Tasks:
1. In `backend/src/channels/channels.controller.ts`:
   - Remove ALL signature verification bypasses in `facebookCallback` (e.g., `state === 'demo-tenant-id'`, `uuidRegex.test(state)`, and dry-run when `state` is missing).
   - Enforce that `state` is required and is verified using `channelsService.verifyOAuthState(state)`. Throw `BadRequestException('Invalid state parameter')` if signature check fails.
2. In `backend/test/channels.e2e-spec.ts`:
   - Add `revokedToken` model mock to `mockPrismaService`:
     ```typescript
     revokedToken: {
       findUnique: jest.fn().mockImplementation(async () => null),
       create: jest.fn().mockImplementation(async (args) => args.data),
     }
     ```
   - In `beforeAll()`, override the `ThrottlerGuard` provider in `Test.createTestingModule` to bypass rate limits during testing:
     ```typescript
     .overrideGuard(ThrottlerGuard)
     .useValue({ canActivate: () => true })
     ```
     Ensure `ThrottlerGuard` is imported from `@nestjs/throttler` in the test file.
   - Update all test cases for `/channels/facebook/callback` to send a valid cryptographically signed state (generated by calling `channelsService.generateOAuthState(tenantId)`).
   - Remove all test cases that assert signature bypasses (like UUID fallback or missing state success).
3. In `backend/test/encryption-robustness.e2e-spec.ts` (created by Challenger 2):
   - Update the assertions to expect that `channelsService.getDecryptedAccessToken` throws `BadRequestException` on decryption failure or key mismatch, rather than expecting it to swallow the error and return the ciphertext.
4. In `backend/test-encryption.js`:
   - Update the local `decrypt` implementation or the assertions to match the production service throwing behavior.
5. Verify your changes by running the test suite:
   - `node node_modules/jest/bin/jest.js --config ./test/jest-e2e.json test/channels.e2e-spec.ts`
   Ensure all tests pass cleanly.

MANDATORY INTEGRITY WARNING:
DO NOT CHEAT. All implementations must be genuine. DO NOT
hardcode test results, create dummy/facade implementations, or
circumvent the intended task. A Forensic Auditor will independently
verify your work. Integrity violations WILL be detected and your
work WILL be rejected.
